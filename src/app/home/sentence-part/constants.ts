export const delay = 0;

//styles
export const styles = {
    weights:[400,800],
    colors:["var(--grey-color)","var(--white-color)"]
}

const content = "Passionné par le monde du digital et de l'entrepreneuriat";
const words = content.split(" ");
const nbWords = words.length;

export const sentence = {
    content:content,
    words:words,
    nbWords:nbWords
}